<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sanyang.mapper.ProjectMapper">
	<insert id="addProject" parameterType="project">
		insert into project values(null,#{dtid},#{proName},#{proAddress},#{beginDate},#{endDate},#{principal},#{isCheck})
	</insert>
</mapper>